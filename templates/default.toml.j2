# default.toml - fallback

[global]
keyboard = "fr"
country = "fr"
timezone = "Europe/Paris"
fqdn = "pve-default"
root-password = "ChangeMe123!"
root-ssh-keys = [
    "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIL1bRjvV4SBhuIS2myssnSzhbanJ0+zAGt7XteJE0hQ0 clinux@rocky9.local"
]
reboot-mode = "power-off"

[network]
source = "from-dhcp"

[disk-setup]
filesystem = "ext4"
disk-list = ["sda"]

[post-install]
script = """
curl -X POST https://gotify.domain.local/message \
  -H "X-Gotify-Key: TOKEN" \
  -F "title=PVE installé (default)" \
  -F "message=MAC {{ primary_mac }}"
"""
